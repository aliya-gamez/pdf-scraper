# DEVELOPMENT

### Windows Run Guide
In PowerShell, run script using command:
```powershell
./setup.ps1
```

If successful, run script with after configuring constants:
```powershell
python ./pdf-scraper.py